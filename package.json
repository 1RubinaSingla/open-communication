{
  "name": "0c",
  "version": "0.0.0",
  "private": true,
  "description": "Open Communication (0_C) — decentralized AI inference + private E2E comms on contributed GPUs",
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "dev:all": "concurrently -n orch,web,worker -c magenta,cyan,green \"pnpm --filter @0c/orchestrator dev\" \"pnpm --filter @0c/web dev\" \"pnpm --filter @0c/worker-native dev\"",
    "orch": "pnpm --filter @0c/orchestrator dev",
    "web": "pnpm --filter @0c/web dev",
    "worker": "pnpm --filter @0c/worker-native dev",
    "typecheck": "turbo run typecheck",
    "build": "turbo run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "tsx": "^4.19.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  }
}
